Standardmäßig läuft die Herausforderung ohne einen Debug-Sidecar. Dein Ziel ist es, die im Pod laufende Binärdatei zu analysieren und das Geheimnis aufzudecken.

1. Deploye die Herausforderung ohne den Sidecar:
   ```
   kubectl apply -f k8s/challenge53/secret-challenge53.yml
   ```
