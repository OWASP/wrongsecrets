За замовчуванням задача запускається без sidecar налагодження. Ваша мета — проаналізувати бінарний файл, що працює всередині pod'а, та розкрити секрет.

1. Розгорніть задачу без sidecar:
   ```
   kubectl apply -f k8s/challenge53/secret-challenge53.yml
   ```
