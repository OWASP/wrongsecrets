Par défaut, le défi s'exécute sans un sidecar de débogage. Votre objectif est d'analyser le binaire s'exécutant dans le pod et de découvrir le secret.

1. Déployez le défi sans le sidecar:
   ```
   kubectl apply -f k8s/challenge53/secret-challenge53.yml
   ```
