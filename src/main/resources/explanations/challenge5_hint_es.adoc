Puedes resolver este desafío con las siguientes soluciones alternativas:

1. Verifica el estado del configmap en Git:
 - Busca dónde en Git almacenamos `secrets-config.yml`.
 - Mira el campo `Data`: ¿qué puedes encontrar allí?

2. Pregunta amablemente usando Kubectl:
 - Ejecuta `kubectl get configmap -n default` y luego `kubectl describe configmap <nombre>`.
